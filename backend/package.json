{
  "name": "ionic-issues-reproduction",
  "version": "0.0.1",
  "author": "rfe-css",
  "description": "Backend",
  "private": true,
  "license": "UNLICENSED",
  "main": "main.js",
  "scripts": {
    "build": "tsc",
    "start": "ts-node-dev --respawn --pretty --transpile-only src/main.ts",
    "debug": "ts-node-dev --respawn --pretty --inspect --transpile-only src/main.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/cors": "^2.8.15",
    "@types/express": "^4.17.20",
    "@types/node": "^20.8.7",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.2.2"
  }
}
